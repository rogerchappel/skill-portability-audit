# Clean Skill

Use this skill for local review. It reads local files only and requires approval before publishing, deploying, sending, deleting, or merging. Verify with npm test and a fixture smoke run.
