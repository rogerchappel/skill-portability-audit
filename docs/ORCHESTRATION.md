# Orchestration

Run `skill-portability-audit` only against local fixtures or user-provided local files. Treat output as a dry-run aid. Any external write described by the output requires separate user approval and a tool-specific plan.
